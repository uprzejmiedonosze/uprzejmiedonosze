{% set leftButton = general.isLoggedIn ? 'menuBack': 'menuMain' %}
{% set rightButton = general.hasApps ? 'menuApplications': 'menuStart' %}

{% extends "base.html.twig" %}
{% block main %}
{{ include('_application.html.twig') }}
{% endblock %}