{% extends "pdf.tex.twig" %}

{% block commands %}
\usepackage{tikz}
\newcommand{\dottedLine}[1]{\tikz\draw [thin, dotted] (0,0) -- (#1,0);}
\pagenumbering{gobble}
\usepackage{fancyhdr}
\pagestyle{fancy}
{% endblock %}

{% block main %}
    {% for app in app %}
        {% include('_hearing.tex.twig') %}
        \clearpage
        {% include('_attachment.tex.twig') %}
    \clearpage
{% endfor %}
{% endblock %}