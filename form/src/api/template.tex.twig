% vi:syntax=tex
\documentclass[10pt]{letter}

\usepackage[pdftex,colorlinks=true,urlcolor=black,linkcolor=black]{hyperref}
\usepackage[pdftex,usenames]{color}
\usepackage[pdftex]{graphicx}
\usepackage[TS1,OT1,T1]{fontenc}
\usepackage[polish]{babel}
\usepackage[utf8]{inputenc}

\usepackage{geometry}
\geometry{
	paper=a4paper,
	top=1.5cm,
	bottom=1.5cm,
	left=2.5cm,
	right=2.5cm,
	%showframe,
}
\renewcommand\familydefault{\sfdefault}
\setlength{\columnseprule}{0.4pt}
\frenchspacing

\newcommand{\n}{ {{ user.name }} }
\date{ Dnia {{ tex.date }} } 
\address{\n{}\\{{ user.address }}\\{{ user.email }}\\{{ user.msisdn }} }

\begin{document}
\begin{letter}{ {\large {{ tex.sm }} } } 

\opening{ {\Large Zgłoszenie wykroczenia\texttt{ {{ number }} } } }
	W dniu {{ tex.date }} roku o godzinie {{ tex.hour }} {{ tex.sex.bylam }} świadkiem pozostawienia
	samochodu o nr rejestracyjnym\texttt{ {{ carInfo.plateId }} }pod adresem {{ address.address }}.
	{{ tex.category }} Sytuacja jest widoczna na załączonych zdjęciach. Zdjęcia {{ tex.sex.wykonalam }}
	samodzielnie. {{ userComment }}

	Nie {{ tex.sex.bylam }} świadkiem samego momentu parkowania, oraz nie wiem jak długo
	pojazd pozostawał na tym miejscu.

	Jestem {{ tex.sex.swiadoma }} odpowiedzialności karnej z art. 233 §1–§3 Kodeksu karnego oraz treści art. 182, 183 i 185 Kodeksu Postępowania Karnego.

	Równocześnie proszę o niezamieszczanie w protokole danych dotyczących mojego miejsca zamieszkania.

	\closing{\textit{Z poważaniem,\\\n{} } }

	{{ tex.carImage }}
	{{ tex.contextImage }}

	{{ tex.plateImage }}
\end{letter}
\end{document}
