% vi:syntax=tex
\documentclass[10pt]{letter}

\usepackage[pdftex,colorlinks=true,urlcolor=black,linkcolor=black]{hyperref}
\usepackage[pdftex,usenames]{color}
\usepackage[pdftex]{graphicx}
\usepackage[TS1,OT1,T1]{fontenc}
\usepackage[polish]{babel}
\usepackage[utf8]{inputenc}

\usepackage{geometry}
\geometry{
	paper=a4paper,
	top=1.5cm,
	bottom=1.5cm,
	left=2.5cm,
	right=2.5cm,
	%showframe,
}
\renewcommand\familydefault{\sfdefault}
\setlength{\columnseprule}{0.4pt}
\frenchspacing

\newcommand{\n}{ {{ app.name_name }} }
\date{ Dnia {{ app.date }} } 
\address{\n{}\\{{ app.user_address }}\\{{ app.user_email }}\\{{ app.user_phone }} }

\begin{document}
\begin{letter}{ {\large {{ app.sm }} } }

\opening{ {\Large Zgłoszenie wykroczenia\texttt{ {{ app.number}} } } }
	W dniu {{ app.date }} roku o godzinie {{ app.hour }} {{ app.sex }} świadkiem pozostawienia
	samochodu o nr rejestracyjnym\texttt{ {{ app.plateId }} }pod adresem {{ app.address }}.
	{{ app.category }} Sytuacja jest widoczna na załączonych zdjęciach. Zdjęcia wykonałem
	samodzielnie. {{ app.userComment }}

	Nie {{ app.sex }} świadkiem samego momentu parkowania oraz nie wiem jak długo
	pozostawał pojazd pozostawał na tym miejscu.

	Jestem świadomy odpowiedzialności karnej z art. 233 §1–§3 Kodeksu karnego oraz treści art. 182, 183 i 185 Kodeksu Postępowania Karnego.

	Równocześnie proszę o niezamieszczanie w protokole danych dotyczących mojego miejsca zamieszkania.

	\closing{\textit{Z poważaniem,\\\n{} } }

	{{ app.carImage }}
	{{ app.contextImage }}

	{{ app.plateImage }}
\end{letter}
\end{document}
