% vi:syntax=tex
\documentclass[a4paper, 10pt]{letter}

\usepackage[pdftex,colorlinks=true,urlcolor=black,linkcolor=black]{hyperref}
\usepackage[pdftex,usenames]{color}
\usepackage[pdftex]{graphicx}
\usepackage[TS1,OT1,T1]{fontenc}
\usepackage[polish]{babel}
\usepackage[utf8]{inputenc}

\addtolength{\hoffset}{-2cm}
\addtolength{\textwidth}{4cm}
\addtolength{\voffset}{-1cm}
\addtolength{\textheight}{2cm}
\renewcommand\familydefault{\sfdefault}
\setlength{\columnseprule}{0.4pt}
\frenchspacing

\newcommand{\n}{ {{ app.name_name }} }
\date{ Dnia {{ app.date }} } 
\address{\n{}\\{{ app.user_address }}\\{{ app.user_email }}\\{{ app.user_phone }} }

\begin{document}
\begin{letter}{ {\large {{ app.sm }} } }

\opening{ {\Large Zgłoszenie wykroczenia\texttt{ {{ app.number}} } } }
	W dniu {{ app.date }} roku o godzinie {{ app.hour }} byłam/em świadkiem pozostawienia
	samochodu o nr rejestracyjnym\texttt{ {{ app.plateId }} }pod adresem {{ app.address }}.
	{{ app.category }} Sytuacja jest widoczna na załączonych zdjęciach. {{ app.userComment }}

	Nie byłem świadkiem samego momentu parkowania oraz nie wiem jak długo
	pozostawał pojazd pozostawał na tym miejscu.

	\closing{\textit{Z poważaniem,\\\n{} } }

	{{ app.carImage }}
	{{ app.contextImage }}

	{{ app.plateImage }}
\end{letter}
\end{document}
