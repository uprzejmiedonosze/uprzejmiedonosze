{% block header %}
    <header>
        <h1>{{ shortTitle ?? title }} 
            {% if general.isStaging %}
                <sup>[STG]</sup>
            {% elseif general.isProd %}
            {% else %}
                <sup>[DEV]</sup>
            {% endif %}
        </h1>
    </header>
{% endblock %}
