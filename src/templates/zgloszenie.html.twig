{# Zalogowany użytkownik (właściciel lub ktoś przeglądający udostępniony link)
   ogląda zgłoszenie w normalnym widoku aplikacji (menu, tabbar, tło /app);
   niezalogowana osoba postronna — w czystym widoku „focus". #}
{% set layout = general.isLoggedIn ? 'app' : 'focus' %}
{% set navbarCtaHref = '/app/new' %}
{% set navbarCtaLabel = 'Nowe zgłoszenie' %}
{% if dialog %}
    {% set leftButton = 'menuBack' %}
    {% set rightButton = '' %}
{% endif %}

{% extends "base.html.twig" %}
{% block main %}
{{ include('_application.html.twig') }}
{% endblock %}